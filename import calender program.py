import calender
calendar.month(year, month) # Print the calendar for the given year 
print(calendar.calendar(year)) # Check if the year is a leap year 
is_leap = calendar.isleap(year) 
print(f"{year} is a leap year: {is_leap}")
